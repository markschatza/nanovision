# Repository Instructions

- Use subagents for non-trivial work when tasks can be split into independent review, validation, research, or implementation slices.
- Keep generated datasets, local credentials, virtual environments, and cache files out of source control.
- Prefer `uv` commands for Python setup, tests, and CLI smoke checks.
